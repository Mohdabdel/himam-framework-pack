# تقرير تدقيق الحالة التنفيذية البرمجية الفعلية
## HIMAM Framework Pack — مراجعة الخطط التربوية

- نوع المهمة: قراءة وتشخيص وتوثيق فقط (لم يُعدَّل أي كود وظيفي).
- تاريخ التدقيق: 2026 (جولة تدقيق داخلية).
- الفرع: `edit/edt-18efc8d7-421b-468e-b4f9-8b9b89768fec`
- آخر التزام: `84e2d1c1` — "Work in progress"
- إصدار حزمة المعرفة المرجعية: HIMAM Pre-Programming Package v1.0 (21 ملفًا).

> اصطلاح الوسم: **مُثبت بالكود** = مقروء حرفيًا من الملفات. **يحتاج تحققًا** = لم يمكن إثباته من الكود أو من حالة المتصفح المفحوصة.

---

## 1) الهوية التقنية والمكدس الفعلي

| العنصر | القيمة المُثبتة |
| --- | --- |
| مدير الحزم / التشغيل | Bun |
| البناء | Vite 7 |
| الإطار | TanStack Start + TanStack Router (توجيه ملفي في `src/routes`) |
| الواجهة | React 19 + TypeScript |
| التنسيق | Tailwind CSS 4 عبر `src/styles.css` (رموز OKLCH) |
| الأيقونات | lucide-react |
| الاختبارات | Vitest (`vitest.config.ts`) — **206 اختبارًا ناجحًا** |
| الضغط/التنزيل | JSZip (شاشة الحزمة المعرفية فقط) |
| قاعدة بيانات خارجية | **لا يوجد** — لا Supabase ولا مصادقة ولا خدمات سحابية |

**خلاصة:** المشروع تطبيق واجهة أمامية بالكامل، يعمل داخل المتصفح، مع مسار خادم واحد معطّل وظيفيًا (انظر §9).

---

## 2) خريطة الملفات التنفيذية

### 2.1 المسارات (`src/routes`)
| الملف | المسار | الدور |
| --- | --- | --- |
| `index.tsx` | `/` | إعادة توجيه إلى `/cases` |
| `framework-package.tsx` | `/framework-package` | تنزيل حزمة ما قبل البرمجة (21 ملفًا + ZIP) |
| `cases.tsx` | `/cases` | تخطيط أب (Outlet) |
| `cases.index.tsx` | `/cases` | لوحة الحالات + نقطة البدء |
| `cases.new.tsx` | `/cases/new` | الخطوة 1: البيانات الأساسية + رفع الخطة |
| `cases.$caseId.tsx` | `/cases/:id` | تخطيط أب (Outlet) — أساس ثبات التنقل |
| `cases.$caseId.index.tsx` | `/cases/:id` | مركز الحالة (Status Hub) |
| `cases.$caseId.sources.tsx` | `.../sources` | الخطوة 2: المصادر |
| `cases.$caseId.ingestion.tsx` | `.../ingestion` | الخطوة 3: تحويل النص |
| `cases.$caseId.extraction.tsx` | `.../extraction` | الخطوة 4: الأدلة والتأكيد البشري |
| `cases.$caseId.scope.tsx` | `.../scope` | الخطوة 5: نطاق المراجعة |
| `cases.$caseId.review.tsx` | `.../review` | الخطوة 6: قرارات المراجع + ختم المراجعة |
| `cases.$caseId.report.tsx` | `.../report` | الخطوتان 7–8: التقرير والإغلاق |
| `api.himam.extract-evidence.ts` | `/api/himam/extract-evidence` | حدّ خادم للاستخراج الذكي — **not_configured** |

### 2.2 نطاقات المنطق (`src/features/himam`)
- `knowledge/` — محمّل حزمة المعرفة، مُحلِّل CSV، تحقق البنية.
- `cases/` — الأنواع، المستودع، `CaseService`، آلة الحالة، الإجراء التالي، تسميات عربية.
- `sources/` — `SourceService`، تخزين ملف الخطة في IndexedDB.
- `ingestion/` — تقطيع النص، المُحلِّلات (TXT/DOCX/PDF)، التهشير، `IngestionService`.
- `extraction/` — تشغيل الاستخراج، مزوّد بديل محلي حتمي، مدقق، فحص تكامل الهوية.
- `scope/` — قواعد المرحلة، خدمة النطاق، `input-impact` (تعريف الرحلة الثمانية).
- `review/` — سجل المعرفة، المحرك الحتمي، المراجعة البشرية، التغطية، خدمة التقرير المُحكوم.
- `audit/` — سجل أحداث تدقيق داخلي لكل حالة.
- `ui/` — `WorkflowShell`، `JourneyStepper`، `StageHeader/Footer`، `NextActionCard`، `StatusBadge`، `ResponsivePanel`، `CollapsibleSection`.

---

## 3) الرحلة التنفيذية الفعلية (8 خطوات)

`JOURNEY_STEPS` في `src/features/himam/scope/input-impact.ts:228` تعرّف بالترتيب:
`basics → sources → text → evidence → scope → review → report → closure`

الربط بالمسارات في `src/features/himam/ui/use-case-journey.ts:24` — الخطوتان 7 و8 تشتركان في `/report`، وخطوة `basics` بلا مسار (تُستكمل عند الإنشاء).

كل شاشة تُغلَّف بـ `WorkflowShell` الذي يعرض المسطرة الثمانية، ويمنع الوصول لخطوة غير مؤهلة ويعيد التوجيه لأول خطوة ناقصة مع تفسير عربي.

---

## 4) ثبات البيانات — ما يُخزَّن وأين

| المخزن | التقنية | المفتاح/الاسم | المحتوى |
| --- | --- | --- | --- |
| مخزن الحالة الرئيسي | localStorage | `himam.pkg1a.store.v1` | الحالات، المصادر، لقطات النطاق، أحداث التدقيق، مصنوعات النص، المقاطع، الأدلة، دورات الاستخراج، فحوص الهوية، نسخ المراجعة، النتائج، نسخ التقرير |
| ملف الخطة والنص | IndexedDB | قاعدة `himam-pkg1a`، مخزنان: `plan_files`, `text_artifacts` | البايتات الفعلية للملف والنص المستخرج |
| تفضيلات إظهار الأقسام | sessionStorage | مفاتيح شاشة المصادر | إظهار/إخفاء المدخلات الاختيارية |

**نتائج جوهرية:**
1. لا يوجد أي ثبات على الخادم — البيانات محلية للمتصفح والجهاز.
2. مسح بيانات المتصفح يمحو الحالات نهائيًا؛ لا استرجاع.
3. المستودع يحتوي طبقة ترحيل (`migrateCases/Sources/Artifacts/Chunks/Evidence`) تحمي المخزن القديم من الانكسار.
4. الأدلة النصية لا تُرسَل لأي خدمة خارجية.

---

## 5) قراءة حالة المخزن الحقيقية في المعاينة

فُحص `localStorage` الحقيقي في جلسة المعاينة المفتوحة (قراءة فقط):

| المعرف المرجعي | العمر | المرحلة | نوع الخطة | الحالة | مرحلة الاستخراج |
| --- | --- | --- | --- | --- | --- |
| تست2 | null | preschool | IEP | draft | not_started |
| تست 5 | null | middle | IEP | scope_confirmed | sources_registered |
| RC-2026-0001 | 11 | **null** | IEP | minimum_inputs_complete | sources_registered |
| RC-2026-0002 | 10 | **null** | IEP | minimum_inputs_complete | text_ready |

أعداد السجلات: 4 حالات، 3 مصادر، 3 لقطات نطاق، 1 مصنوع نص، 229 مقطعًا، **0 أدلة**، **0 نسخ مراجعة**، **0 نتائج**، **0 تقارير**، 12 حدث تدقيق.

---

## 6) تشخيص عرض «IEP · غير محددة»

**السبب الجذري مُثبت بالكود والبيانات:**
- شاشة الإنشاء (`cases.new.tsx:69`) تقبل **العمر أو المرحلة** — أحدهما يكفي.
- عند إدخال العمر فقط، تُحفظ `ageYears` وتبقى `phaseId = null` — `CaseService.createCase` يخزّن المُدخل كما هو ولا يستنبط المرحلة من العمر (لا توجد أي دالة `ageToPhase` في الكود).
- بطاقة الحالة تعرض `planType` ثم `phaseLabelAr(phaseId)`؛ ومع `phaseId = null` تُنتَج «غير محددة» (`case-labels.ts`).
- البيانات الحقيقية تؤكده: RC-2026-0001 و RC-2026-0002 لهما عمر وبلا مرحلة.

**إذن العرض ليس خللًا في التخزين، بل نتيجة غياب اشتقاق المرحلة من العمر.** `phase-rules.ts` يتعامل مع المراحل ترتيبيًا فقط ويصرّح بعدم تثبيت حدود عمرية رقمية — فالاشتقاق قرار معرفي غير مُتخذ في الحزمة، لا نقص برمجي عارض.

**الأثر:** كل قاعدة نطاق مبنية على بوابة عمرية تُرجِع `null` (غير قابلة للحسم) عندما تكون المرحلة `null`، مما قد يوسّع بند «غير قابل للمراجعة» بدل تصنيفه بدقة.

---

## 7) القرارات البشرية وأعدادها

- `ReviewVersionService.runEngine` يشغّل المحرك الحتمي، ويولّد `ReviewFinding` لكل معيار داخل النطاق المؤكَّد، ويعلّم النسخ السابقة `isStale`.
- عدد «بانتظار قرار» = `pendingHumanDecisionCount` = عدد النتائج غير المحسومة (`review-coverage-service.ts:67`, `governed-report-service.ts:328`).
- إذن أي رقم كبير (مثل 54) هو **عدد معايير النطاق المؤكَّد**، وليس عددًا ثابتًا: كل معيار مُتاح ينتج نتيجة تحتاج قرارًا بشريًا (`accept | modify | reject | request_more_information`).
- `HumanReviewService.applyDecisions` يطبّق القرارات **ذريًّا** (تحميل واحد → تعديل → حفظ واحد) ويسجّل حدث تدقيق لكل قرار، ويرفض القرار على نتيجة قديمة أو حالة مغلقة.
- **يحتاج تحققًا:** لم يمكن إعادة إنتاج الرقم 54 في المخزن المفحوص لأنه يحتوي 0 نتائج (لم تُشغَّل المراجعة في تلك الجلسة).

---

## 8) بوابات إصدار التقرير (المُثبتة حرفيًا)

`GovernedReportService.canGenerateGovernedReport` يمنع الإصدار بأحد الأسباب التالية بالترتيب:
1. `case_not_found`
2. `case_closed_read_only`
3. `scope_needs_reconfirmation`
4. `extraction_not_confirmed` (مرحلة الحالة ليست `extraction_confirmed`)
5. `identity_conflict_unresolved`
6. `no_review_version`
7. `review_not_completed` (المراجعة غير مختومة)
8. `review_stale`
9. `critical_findings_pending` (نتيجة حرجة بلا قرار)
10. `evidence_drift_detected` (بصمة الأدلة تغيّرت بعد تشغيل المحرك)

سلوك الأزرار في `cases.$caseId.report.tsx`: «إصدار مسودة» معطّل ما لم تُفتح البوابة؛ «الاعتماد النهائي» يعمل على مسودة فقط؛ «الطباعة» و«إغلاق الحالة» يعملان على نسخة **معتمدة** فقط. وأي إعادة تشغيل للمحرك تُصيّر التقارير `stale`.

بوابة فتح المراجعة (`canOpenReview`) تشترط: `scope_confirmed` + عدم حاجة إعادة تأكيد + `extraction_confirmed` + عدم تعارض هوية + وجود أدلة مؤكّدة **أو** بنود نطاق قابلة للتقرير عنها.

---

## 9) الذكاء الاصطناعي وOCR — الحالة الفعلية

- `/api/himam/extract-evidence` يعيد `availability: "not_configured"` على GET، ويرفض كل POST بـ `errorCode: "not_configured"`. **لا مفاتيح، لا مزوّد، لا استدعاء خارجي.**
- البديل الفعلي هو `local-fallback-provider.ts`: استخراج **حتمي محلي** بلا نموذج، يُنتج مقتبسات حرفية من المقاطع فقط.
- كل دليل يبقى `pending` حتى يؤكده بشري (`EvidenceReviewStatus`).
- **لا OCR** — المستندات الممسوحة تُصنَّف `text_unavailable` وتُغلق بقرار مراجع مُسجَّل (`unavailableResolution`).
- **لا درجات جودة** ولا نسب مئوية في المخرجات.

---

## 10) جودة الكود والفحوص الآلية

| الفحص | النتيجة |
| --- | --- |
| اختبارات Vitest | **206/206 ناجحة** |
| بناء الإنتاج | ناجح |
| تحقق حزمة المعرفة (`scripts/validate-himam-package.mjs`) | ناجح — 21 ملفًا متسقة |
| ESLint / Prettier | **فاشل (خروج 1)** — نحو 520 ملاحظة، غالبها تنسيق Prettier في `src/routes/cases.new.tsx` و`src/routes/index.tsx` |

الاختبارات تشمل: 1A/1B/1C، بوابة تصحيح UX، رحلة شاملة (E2E)، ثبات ملف الخطة، رموز الواجهة، فجوات التدقيق، الإجراء التالي، بوابة إغلاق المرحلة الأولى.

---

## 11) حزمة ما قبل البرمجة

`src/lib/himam-package/files.ts` يستورد الملفات الـ21 حرفيًا (`?raw`) من `/himam-preprogramming-package-v1.0/`، فيُنزَّل نفس البايتات المُتحقَّق منها. ملفات CSV تُقدَّم مع BOM بترميز UTF-8 لضمان القراءة العربية، وتُحزَّم في `HIMAM_PreProgramming_Package_v1.0.zip` عبر JSZip.

---

## 12) الفجوات المرصودة (بدون تعديل)

| # | الفجوة | الأثر | الطبقة |
| --- | --- | --- | --- |
| F1 | لا اشتقاق للمرحلة من العمر → «غير محددة» | تصنيف نطاق أضعف + عرض ناقص | معرفي/منطقي |
| F2 | الثبات محلي بالكامل (localStorage + IndexedDB) | فقدان البيانات عند مسح المتصفح؛ لا مشاركة بين أجهزة | معماري |
| F3 | ESLint/Prettier فاشل | ضوضاء في المراجعة وخطر تعارضات | جودة |
| F4 | لا مزوّد استخراج حقيقي | الاستخراج حتمي محلي فقط | مقصود بالحزمة |
| F5 | لا OCR للمستندات الممسوحة | مسار `text_unavailable` يدويًا | مقصود بالحزمة |
| F6 | لا مصادقة ولا أدوار | لا فصل مستخدمين ولا مسؤولية موثّقة للفاعل | معماري |
| F7 | عدد القرارات البشرية قد يكون كبيرًا جدًا لكل حالة | عبء تشغيلي على المراجع | تجربة استخدام |

---

## 13) ما هو جاهز فعليًا للتجريب

- إنشاء حالة برفع خطة إلزامي، مع معرف مرجعي مولَّد تلقائيًا (`RC-YYYY-XXXX`).
- تسجيل مصادر إضافية اختيارية مع إظهار أثر كل مدخل.
- تحويل TXT/DOCX/PDF إلى نص ومقاطع مع تهشير ومواضع مرجعية.
- استخراج أدلة حتمي محلي + تأكيد/رفض بشري + فحص تكامل الهوية.
- تثبيت نطاق مراجعة وتأكيده مع تصنيف كل معيار وسببه.
- تشغيل المحرك الحتمي، قرارات بشرية فردية وجماعية، ختم المراجعة.
- إصدار تقرير مُحكوم، اعتماده، طباعته، ثم إغلاق الحالة.

## 14) ما هو غير جاهز

- أي تشغيل متعدد المستخدمين أو متعدد الأجهزة.
- استخراج ذكي حقيقي، OCR، درجات جودة.
- سجل متعلم أو ملف شخصي — **غائب تمامًا وبقصد**.
- تصدير/استيراد بيانات الحالات، أو نسخ احتياطي.

## 15) تقييم الجاهزية

| البُعد | التقييم |
| --- | --- |
| اكتمال الرحلة من الإنشاء إلى التقرير | **جاهز للتجريب المحدود** |
| حَكَمِيَّة القرار (بوابات صريحة + تدقيق) | **قوي** |
| ثبات البيانات للتشغيل الحقيقي | **غير جاهز** (محلي فقط) |
| الالتزام بحدود الحزمة (لا AI/OCR/درجات/سجل متعلم) | **مُلتزم بالكامل** |
| نظافة الكود الآلية | **يحتاج تصحيح تنسيق** |

**الخلاصة:** المنتج في وضع «نموذج تشغيلي حَكَمي كامل الرحلة على جهاز واحد». الحاجزان الرئيسان قبل أي تجريب ميداني: اشتقاق المرحلة من العمر (F1) وثبات البيانات المركزي (F2).
